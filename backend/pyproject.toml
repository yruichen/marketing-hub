[project]
name = "marketing-hub-backend"
version = "0.1.0"
description = "Django API, workflow runtime, and AI provider gateway for Marketing Hub"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "celery>=5.5.0",
    "cryptography>=46.0.0",
    "django>=6.0.5",
    "django-cors-headers>=4.9.0",
    "djangorestframework>=3.17.1",
    "gunicorn>=23.0.0",
    "psycopg[binary]>=3.2.0",
    "redis>=5.2.0",
    "pydantic>=2.6.0",
    "pyyaml>=6.0.0",
]
