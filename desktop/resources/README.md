# Desktop release resources

Release-owned icons, entitlements, and installer assets belong here. Do not add
private signing certificates, provisioning profiles, or credentials to the
repository.

Before the first signed public release, provide reviewed `.icns`, `.ico`, and
Linux PNG assets derived from the canonical brand mark, then wire them into
`forge.config.cjs` in the same change.
